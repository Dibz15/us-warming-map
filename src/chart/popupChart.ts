// Small multiple line chart shown on hover/tap: annual max and min bounds
// (as a filled band or two lines) plus a mean trend line, for one county.
//
// Not implemented yet — scaffolding only.

import type { CountyTrend } from "@/types";

export function renderPopupChart(_container: HTMLElement, _county: CountyTrend): void {
  throw new Error("not implemented");
}
